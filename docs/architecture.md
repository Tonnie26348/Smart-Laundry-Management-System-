# Architecture

The system utilizes a clean, scalable, feature-based architecture to ensure separation of concerns and maintainability.

- **Client:** React (SPA/PWA)
- **API:** Supabase Client (REST/Realtime)
- **Data Layer:** PostgreSQL (RLS-enforced)
